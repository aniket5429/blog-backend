export default function globalSetup(): void {
    // This function is called once before all test suites
    // This is where you would put any setup code for your test suite
    // For example, you could start a server, spin up a Docker container, etc.
    console.log('global setup')
}
